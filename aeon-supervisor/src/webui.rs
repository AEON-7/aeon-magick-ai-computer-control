//! Placeholder. SvelteKit produces a static `build/` directory that ships
//! into `/usr/share/aeon/web/`. `api.rs` mounts ServeDir there as the
//! fallback for non-`/api/*` routes — no Rust code needed here yet.
//!
//! Future: SSE / WebSocket push for live state.
